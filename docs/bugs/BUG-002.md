# BUG-002

## Bug Title

Background sound is white-noise-like, ignores volume, continues after stop, and overpowers speech after meanings.

## First Reported

2026-07-16

## User Report

The user reported a new audio/background-sound bug:

> 新的问题是：
> 1. rain 不像rain，是白噪音
> 2. 声音音量无法被控制
> 3. 点击停止之后还是在放白噪音
> 4. 拿 中文、japanese、food作为例子，播放完米饭 餐 之后白噪音就会变得特别大声，盖住声音，这种情况在选择别的combination也有复现

## Environment

- Project: LingoSleep
- Repository path: `/Users/ariac/Documents/learn japanese or korean while sleeping`
- Current branch at intake: `main`
- Frontend: React + Vite + TypeScript
- Audio stack: Web Speech API and Web Audio API generated background sounds
- Hosting: Cloudflare Pages
- Production URL: `https://lingosleep.pages.dev`
- Local timezone at intake: Asia/Singapore

## Reproduction Steps

Initial user-reported paths:

1. Select background sound `rain`.
2. Start playback and listen to the background sound.
3. Adjust voice/background volume controls.
4. Click Stop while background audio is playing.
5. Select native language Chinese, target language Japanese, topic Food.
6. Start playback and listen after the app says the meaning `米饭；餐`.
7. Repeat with other language/topic combinations.

## Expected Result

- `rain` should sound recognizably like rain, not generic white noise.
- Voice and background volume controls should affect the corresponding audio.
- Stop should immediately stop voice and background audio.
- Background audio should remain ducked or balanced around speech and should not suddenly become loud enough to cover the voice.

## Actual Result

- `rain` sounds like white noise.
- Audio volume cannot be controlled.
- After clicking Stop, white-noise-like background audio keeps playing.
- With Chinese + Japanese + Food, after `米饭；餐` is spoken, the background noise becomes very loud and covers the voice. Similar behavior is reproducible with other combinations.

## Severity

High. This affects the core audio playback experience and can continue unwanted sound after Stop.

## Current Status

READY_TO_DEPLOY

## Investigator Conclusion

Bug Investigator analytically confirmed multiple frontend audio issues in `src/main.tsx`:

- `rain` is generated as raw random noise with occasional spikes, so it is effectively white noise.
- `startSession()` starts background audio, but `stopSession()` does not call `background.stop()`, leaving the audio source/context running after Stop and media-session pause.
- In Recall mode, background ducking is released immediately after the native meaning and before the recall pause, so the background ramps back to full configured volume after phrases like `米饭；餐`.
- Active volume behavior is partly stale/scheduled: background gain updates do not cancel existing ramps, and voice playback uses config captured at session start.

Production/cache re-investigation after user pushed:

- Direct production fetches show `https://lingosleep.pages.dev` serves `/assets/index-ChOD3y-N.js`, matching the local built bundle that contains the BUG-002 fixes.
- `origin/main` contained the BUG-002 fix merge when checked.
- `public/public-sw.js` still uses fixed cache name `lingosleep-v1` and cache-first behavior for every GET. Existing browsers can keep receiving cached old `/`, `/index.html`, or old JS even after Cloudflare Pages deploys new assets.
- Real-audio gap investigation found that mocked tests do not cover an already-installed stale Service Worker, player-screen volume accessibility, or playback using latest config while already running.

## Root Cause

Frontend audio lifecycle and synthesis issues in `src/main.tsx`:

1. `createNoiseSource()` treats `rain` as unfiltered white noise with random amplitude spikes.
2. `stopSession()` cancels speech but does not stop background audio.
3. Ducking restores background to full configured volume during recall pauses, which can overpower the next voice when the background texture is noise-like.
4. Active gain updates do not cancel scheduled ramps before applying new volume.

Reopened production root-cause candidates:

1. Existing client-side Service Worker cache can keep serving the old app bundle despite Cloudflare serving the new bundle to fresh requests.
2. Player screen does not expose voice/background volume controls while audio is playing, so users cannot control volume during the problematic moment.
3. Active playback still relies on render-time closures for config/background behavior in ways that need stronger real-use regression coverage.
4. Rain remains procedurally generated from noise and may still not meet user expectations without a better audio texture or real audio asset.

Latest local fix:

1. `public/public-sw.js` now uses a new cache version and network-first handling for navigation/document requests, and scripts/styles no longer prefer stale cache.
2. The player screen exposes Voice and Background volume sliders during active playback.
3. Active speech uses the latest `voiceVolume` through a ref, and background ducking uses the latest `backgroundVolume` while preserving ducked state during speech.

## Fix Commit

Merged locally on `main` via `merge: bug-002 audio lifecycle fix`.

Fix commits on `bug/bug-002-audio-fix`:

- `fd3ca62 fix: resolve bug-002 audio lifecycle`
- `9764ab8 fix: stop playback sequence after session stop`
- `e6a844b fix: isolate stopped playback sessions`
- `5d2c915 fix: guard fade completion by session token`

Latest local working-tree fix is pending commit after reopened production/cache investigation.

## Regression Test

Implemented across:

- `4da190a test: add bug-002 audio lifecycle regression`
- `36f55ee test: cover bug-002 audio balance regression`

The Playwright regression mocks Web Audio and Web Speech:

- Stop test failed on pre-fix code: `speechSynthesis.cancel()` was called, but background source `stop()` and `AudioContext.close()` were not called.
- Recall balance test failed on pre-fix code because background restored to full volume immediately after the native meaning.
- Added regression coverage for Stop during Recall gap, Stop then immediate Start, fade-out then immediate Start, and background volume slider gain control.
- Added reopened BUG-002 coverage for Service Worker update strategy, player-screen volume controls, and active playback volume changes preserving ducked background gain.
- Rain audio quality still needs manual audible acceptance or future deterministic generator extraction.

## Review Result

Approved after three review iterations.

- First review found Major: stopped `speakItem()` could continue after Stop.
- Second review found Major: Stop then immediate Start could let an old async chain resume under the new session.
- Third review found Major: old fade-out could call `stopSession(true)` after a new session starts.
- Final review: no Blocker, no Major. Minor residual risk: rain quality requires manual listening acceptance.

## Deployment Result

Local integration passed on `bug/bug-002-integration` at `5d2c915`.

- `npm install`: passed.
- `npm run build`: passed.
- `npm run e2e`: passed after sandbox escalation; 8/8 tests passed after local merge to `main`.
- Latest local validation after reopened fix: `npm run build` passed and `npm run e2e` passed after sandbox escalation; 12/12 tests passed.
- `npm test`, `npm run lint`, `npm run typecheck`: not configured in `package.json`.
- Manual local browser check passed for Japanese + Simplified Chinese + Food: player showed `ご飯 / ごはん / 米饭；餐 · gohan`; after Stop, timers stayed fixed.
- Rain听感: manual real-audio listening still required.
- Production deployment verification reopened. User reports that after pushing, all original BUG-002 symptoms still reproduce in production/real use. Local `main` and `origin/main` now both point to `f0a1a4e`, so the next investigation must determine whether Cloudflare Pages deployed the commit, whether a service worker/browser cache is serving an old bundle, or whether the local mocked tests missed real audio behavior.

## Closed At

Pending.

## Timeline

- 2026-07-16: BUG-002 created from user report. Status set to INVESTIGATING. Manager will start Bug Investigator and Regression Test Worker before any fix.
- 2026-07-16: Bug Investigator returned SUCCESS with root-cause evidence in `src/main.tsx` for rain generation, Stop lifecycle, ducking behavior, and volume scheduling.
- 2026-07-16: Regression Test Worker returned SUCCESS and added failing Playwright lifecycle regression. Manager committed it on `test/bug-002-regression` as `4da190a`.
- 2026-07-16: Status changed to FIX_IN_PROGRESS. Manager created `bug/bug-002-fix` worktree from the failing regression branch and assigned Fix Worker.
- 2026-07-16: Manager added stronger audio-balance regression on `test/bug-002-audio-balance` as `36f55ee`, covering recall-pause ducking and background volume gain control.
- 2026-07-16: Fix Worker implemented audio lifecycle/rain/ducking fixes, then addressed three Review Major findings with session-stop guards and session tokens.
- 2026-07-16: Code Review Worker approved final branch with no Blocker or Major.
- 2026-07-16: Integration and Test Worker passed build, e2e, BUG-001 adjacent regression, and local manual BUG-002 checks.
- 2026-07-16: Manager merged BUG-002 fix locally to `main` and reran `npm install`, `npm run build`, and `npm run e2e`; all passed after e2e sandbox escalation.
- 2026-07-16: Status changed to READY_TO_DEPLOY pending GitHub push, Cloudflare Pages deployment, production verification, and real-audio rain listening acceptance.
- 2026-07-16: Deployment Verification Worker returned BLOCKED because local commits are not confirmed on GitHub and GitHub HTTPS credentials are unavailable. Status changed to BLOCKED.
- 2026-07-16: User reported that after pushing, all original BUG-002 symptoms still reproduce: rain sounds like white noise, volume cannot be controlled, Stop does not stop background noise, and background becomes loud after `米饭；餐`. Manager confirmed local `main` and `origin/main` now both point to `f0a1a4e`. Status changed to INVESTIGATING for production/real-audio re-investigation.
- 2026-07-16: Production/cache investigation confirmed production direct requests serve the fixed `index-ChOD3y-N.js`, but `public-sw.js` still has fixed `lingosleep-v1` cache-first behavior, making stale client-side Service Worker cache the strongest explanation for unchanged symptoms after push.
- 2026-07-16: Real-audio gap investigation found additional gaps: no volume controls on player screen, active playback config closure risk, shallow Web Audio mocks, and rain quality still requiring stronger acceptance.
- 2026-07-16: Manager implemented reopened fix locally: Service Worker cache strategy update, player-screen volume controls, latest-volume refs for active playback, and ducked gain preservation during live background-volume changes. `npm run build` passed; `npm run e2e` passed with 12/12 tests after sandbox escalation. Status changed to READY_TO_DEPLOY pending commit/push and production real-audio verification.
